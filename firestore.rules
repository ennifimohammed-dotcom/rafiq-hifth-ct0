rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single-teacher app — open access.
    // Data is protected by obscurity (project ID + document paths).
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
